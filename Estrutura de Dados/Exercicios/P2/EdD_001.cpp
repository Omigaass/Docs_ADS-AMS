#include <stdio.h>
#include <stdlib.h>
using namespace std;

//Dada uma pilha qualquer contendo os itens 3,9,5,1 (inseridos nessa ordem) descreva o conteúdo do sitns da pilha ap´s as seguintes operações: push(2), pop(), top(), pop(), push(7), pop(), pop(), push(4), pop(), pop(), push(8), push(6), pop()

int main(){

    return 0;
}